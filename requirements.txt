# numpy pinned <2: the cluster's torch 2.3.1 is built against numpy 1.x, and
# numpy 2.x breaks scikit-image's nlmeans denoiser (our strongest extractor).
numpy>=1.24,<2
pillow>=10.0
requests>=2.31
torch>=2.0
torchvision>=0.15
lpips>=0.1.4
scipy>=1.10
scikit-image>=0.21
PyWavelets>=1.4          # required by scikit-image nlmeans (sigma est.) + wavelet denoisers
opencv-python-headless>=4.8,<5
