# Claude Code local state
.claude/
CLAUDE.md

# Task materials (PDFs, HPC guide) — not needed at runtime, not committed
Relevant/

# Provided templates pulled from HuggingFace (task_template.py, submission.py) —
# kept locally for reference; our own code lives in src/ and scripts/
Reference/
reference/

# Internal contingency plans / notes — not part of the deliverable
plans/

# Dataset (~120 MB extracted) — fetched from HuggingFace, never committed
data/
*.zip

# Pipeline outputs
submissions/
runlogs/
checkpoints/
artifacts/
*.npz
*.npy

# Third-party model weights — downloaded locally, never committed (>100MB, GitHub rejects).
# The pipeline re-embeds/builds everything on the laptop; the repo only needs our code.
InvisMark/
*.ckpt
*.pt
*.pth
*.onnx

# Python artifacts
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# OS / editor metadata
.DS_Store
.idea/
.vscode/
*.swp
*.swo
